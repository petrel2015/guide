---
question: "事务隔离级别"
tags: ["后端"]
---

# 事务隔离级别
事务隔离级别有四种，从低到高分别是：

- 读未提交：可以读取其他事务未提交的数据，可能导致脏读。
- 读已提交：只能读取已提交的数据，避免脏读，但可能出现不可重复读。
- 可重复读：同一事务中多次读取同一数据结果一致，避免不可重复读，但可能出现幻读。
- 可串行化：最高隔离级别，完全串行执行事务，避免所有并发问题，但性能开销最大。

大多数数据库默认使用的是读已提交或可重复读，比如 MySQL 的 InnoDB 默认是可重复读。