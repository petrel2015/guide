---
question: "ACID是什么"
tags: [ "后端" ]
---

数据库事务的 ACID 是指**事务的四个关键特性**：

- A（Atomicity，原子性） ：事务是一个不可分割的操作单元，要么全部执行成功，要么全部失败回滚。
- C（Consistency，一致性） ：事务执行前后，数据库都必须保持一致性，比如满足所有约束条件。
- I（Isolation，隔离性） ：多个事务并发执行时，彼此之间不能互相干扰，每个事务的中间状态对其他事务不可见。
- D（Durability，持久性） ：事务一旦提交，其结果就会永久保存，即使系统崩溃也不会丢失。