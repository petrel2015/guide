问题单解决
#STAR
关键词：沟通能力，问题解决能力，团队合作精神，情绪管理，适应性和灵活性，领导力。

冲突：我和测试观点不一致

情境 (Situation):
SIT全量测试时，测试测出了一个兼容性问题。25.1有2个新需求，有互相冲突的点。我作为一个需求的特性PL已经提前识别到风险，跟SE沟通，提示这块潜在风险。SE打包票说不会有这个场景，因为另一个需求还没开发，我就没有强硬的让SE在规格里，或者其他地方强调这个结论。但是测试在SIT测试时将两个场景叠加测。这块就出现了一个不兼容点，导致spark任务计算失败。这个测试场景是2个流程，第一个15分钟流程通过读一个自定义路径，经过spark计算，输出到内部路径，这是一个Spark计算任务。接下来的是采集任务（copy到自定义路径），然后还有SaprkSQL加载任务（入库）。将采集任务结果用作60分钟流程的spark计算任务，后续接了60min的采集和加载任务。

这是出现的是60min任务spark计算失败，原因是计算xml转了内容，原本需要的参数没有了，没能兼容。

按照spark模块的理解，这种自定义路径spark汇聚计算场景不应该转hudi。但是dolas认为都要改。

僵持不下，上ccb裁决。dolas认为这种过一个计算任务输出两个任务的场景应该改。但不是spark模块想要的结论，但这个问题单目前看着能走掉，为了关单。dolas改完了，然后发现有后续的问题，采集任务采集失败，因为路径解析错误，逗号不能正确解析。这样spark60min任务还是会失败，因为读不到数据。对于测试来说这个场景还是不通，不能关单。另外加载任务失败是因为设计态多租数据源和单租数据源存在兼容性问题，是dolas编排的问题，dolas知道这块历史有坑，不好改，牵一发动全身。所以现在这个问题单不好关。同时我们又进行了一次ccb，希望裁整个场景就不支持。dolas这次被说服了，理解，但是分析后需要22人天的工作量。需要下个版本专需求，在现有规划的需求加AR。这样版本SE能接受，测试SE能接受，把问题单改版本。下个版本再继续跟踪，如果完不成下个版本还需要上ccb。但是目前所有人都满意了，DI满足了，po满意了。单子不直接关掉。采集，加载，spark计算，dolas开发都满意了，因为不需要很紧急的解决这个风险很大的问题。

但是问题依然真正的没有闭环，这3个隐藏的问题还是需要解决，因为问题单没有关闭，只是改版本，下个版本测试回归的时候用同样的app去测试就会发现后续问题了。所以我把这几个问题同步给了这几个组的SE，PL，对应问题定位的开发。下个版本再解决不了别怪测试提单了，别怪我没有同步消息了。我能提前识别到的风险已经告知了。

任务 (Task):

行动 (Action):

结果 (Result):
